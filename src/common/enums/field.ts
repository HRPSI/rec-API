export enum FieldHistorySortField {
	CreatedAt = 'createdAt',
	FieldId = 'fieldId',
	Action = 'action',
	FieldName = 'fieldName',
	UserId = 'userId',
	UserName = 'userName',
	UserEmail = 'userEmail',
}

export enum FieldBehaviorEnum {
	SINGLE = 'single',
	BULK = 'bulk',
}
