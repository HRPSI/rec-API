export enum ReportType {
	MASTER_DATA = 'master_data',
}

export enum ReportFileExtension {
	PDF = 'pdf',
	EXCEL = 'xlsx',
}

export enum CellPattern {
	SOLID = 'solid',
	NONE = 'none',
}

export enum CellAlignment {
	LEFT = 'left',
	CENTER = 'center',
	RIGHT = 'right',
	JUSTIFY = 'justify',
}

export enum CellVerticalAlignment {
	TOP = 'top',
	MIDDLE = 'middle',
	BOTTOM = 'bottom',
}

export enum CellBorderStyle {
	THIN = 'thin',
	MEDIUM = 'medium',
	THICK = 'thick',
	DASHED = 'dashed',
	DOTTED = 'dotted',
	DOUBLE = 'double',
	NONE = 'none',
}

export enum CellFontWeight {
	NORMAL = 'normal',
	BOLD = 'bold',
}

export enum CellFontFamily {
	ARIAL = 'Arial',
	CALIBRI = 'Calibri',
	TIMES_NEW_ROMAN = 'Times New Roman',
	VERDANA = 'Verdana',
	HELVETICA = 'Helvetica',
}
